import React from "react";
import ModalBody from "react-bootstrap/ModalBody";

export function EventModalBody({children}) {
  
  return (
        <ModalBody>
            {children}
        </ModalBody>  
  );
}

